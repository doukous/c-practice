# Identification of incorrect functions

The calls that won't work:

- b : the conversion specification needs a string but is given a character
- c : the conversion specification needs a character but is given a string
- e : the printf function takes a string litteral as parameter 
- h : putchar function takes a string as parameter
- i : puts function takes a string litteral as parameter